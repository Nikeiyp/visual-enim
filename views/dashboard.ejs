<!DOCTYPE html>
<html>
<head>
    <title>Visual-Enim | Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        /* CSS Global */
        body { margin: 0; font-family: 'Arial', sans-serif; background: white; }
        
        /* Navbar */
        nav { display: flex; align-items: center; padding: 20px 40px; border-bottom: 1px solid #eee; }
        .logo { font-weight: 900; font-size: 1.2rem; margin-right: 40px; }
        .nav-links a { text-decoration: none; color: #888; font-weight: 600; margin-right: 20px; transition: 0.3s; }
        .nav-links a:hover { color: black; }
        .nav-links a.active { color: black; border-bottom: 2px solid black; padding-bottom: 19px; }

        /* Content Style */
        .container { text-align: center; padding: 40px 20px; max-width: 1000px; margin: 0 auto; }
        h1 { font-size: 3rem; margin-bottom: 0; letter-spacing: -1px; }
        .subtitle { color: #aaa; font-weight: 700; font-size: 2rem; margin-top: -10px; margin-bottom: 40px; }
        
        /* Style Kotak Gambar */
        .gray-box {
            width: 100%; height: 400px;
            border-radius: 15px; margin-bottom: 60px;
            overflow: hidden; /* Agar gambar tidak keluar dari sudut yang membulat */
            box-shadow: 0 10px 20px rgba(0,0,0,0.1); /* Tambah bayangan sedikit biar keren */
        }

        /* Style Gambar di dalam kotak */
        .gray-box img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Gambar akan memenuhi kotak tanpa terdistorsi (terpotong jika perlu) */
            object-position: center; /* Fokus gambar di tengah */
        }

        .quote { font-weight: 700; font-size: 1.2rem; max-width: 600px; margin: 0 auto; line-height: 1.5; }
        .verse { color: #888; font-size: 0.9rem; margin-top: 10px; display: block; }
    </style>
</head>
<body>
    <nav>
        <div class="logo">Visual-Enim</div>
        <div class="nav-links">
            <a href="/dashboard" class="active">Home</a>
            <a href="/battery">Battery Check</a>
            <a href="#">Schedule</a>
        </div>
    </nav>

    <div class="container">
        <h1>Visual-Enim</h1>
        <div class="subtitle">Dashboard</div>
        
        <div class="gray-box">
            <img src="https://images.unsplash.com/photo-1492539161849-b2b18e79c85f?q=80&w=2070&auto=format&fit=crop" alt="Dashboard Image">
        </div>

        <div class="quote">
            “Whatever you do, work at it with all your heart, as working for the Lord, not for human masters.”
            <span class="verse">(Colossians 3:23)</span>
        </div>
    </div>
</body>
</html>