<!DOCTYPE html>
<html>
<head>
    <title>Visual-Enim | Home</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            margin: 0; padding: 0;
            height: 100vh;
            display: flex; flex-direction: column;
            justify-content: center; align-items: center;
            font-family: 'Arial', sans-serif;
            background: white;
        }
        h1 { font-size: 4rem; font-weight: 800; margin: 0; letter-spacing: -2px; }
        p { font-size: 1.5rem; color: #888; margin-top: -5px; font-weight: 600; }
        .btn-start {
            background: black; color: white;
            padding: 10px 30px; border-radius: 50px;
            text-decoration: none; font-weight: bold;
            margin-top: 30px; transition: transform 0.2s;
        }
        .btn-start:hover { transform: scale(1.05); }
    </style>
</head>
<body>
    <h1>Visual-Enim</h1>
    <p>Home-Web</p>
    <a href="/dashboard" class="btn-start">Start</a>
</body>
</html>